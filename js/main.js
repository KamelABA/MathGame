/* ============================================
   MAIN — Entry point
   ============================================ */

document.addEventListener('DOMContentLoaded', () => {
    engine.init();
    console.log('🎮 MathDesk initialized!');
});
